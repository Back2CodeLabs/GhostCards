# Dépendances de développement uniquement (tests) — jamais nécessaires sur
# l'OptiPlex en production, volontairement séparées de requirements.txt.
# Installation : pip install -r requirements.txt -r requirements-dev.txt
pytest>=8
