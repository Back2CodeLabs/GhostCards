fastapi==0.115.*
uvicorn[standard]==0.32.*
pronotepy>=2.15
apscheduler==3.10.*
python-dotenv==1.0.*
anthropic>=0.40
pydantic>=2
itsdangerous>=2.1
python-multipart>=0.0.9
cryptography>=42
# Décodage des QR codes Pronote uploadés lors du pairage élève (voir
# BackEnd/app/main.py::pairer_eleve_pronote) — headless : pas de serveur X
# nécessaire sur l'OptiPlex.
opencv-python-headless>=4.9

# OCR local (moteur par défaut, voir Services/config.py::OCR_ENGINE).
# Nécessite un venv Python <=3.13 : PaddlePaddle n'a pas de wheels pour
# Python 3.14 (voir HANDOFF.md, section OCR, pour la procédure de
# changement de version sur l'OptiPlex avant `pip install -r requirements.txt`).
paddlepaddle==3.3.1
paddleocr==3.7.0
